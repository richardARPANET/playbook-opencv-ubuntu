# playbook-opencv-ubuntu

Ansible playbook to install opencv on ubuntu 14.04.

```
ansible-playbook -i 'localhost,' --connection=local -vvvv
```

**Note:** The `make` command will take **HOURS** to run. Please be patient.
